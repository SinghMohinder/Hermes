from multiprocessing.managers import BaseManager

class reporterClient(BaseManager):
    '''
    proxy Process for communication between testReporter<--> Reporter (hermesNode)

    '''
    pass